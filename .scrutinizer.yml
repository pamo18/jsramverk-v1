build:
  nodes:
    functional-tests:
      environment:
        selenium: true
    coverage:
      tests:
        override:
          - command: npm test
            coverage:
              file: coverage/clover.xml
              format: clover
